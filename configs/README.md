# Generated Configs

This directory will contain your project-specific configurations generated by `scripts/generate_config.sh`.

**Example:**
```bash
./scripts/generate_config.sh my_character ohwx
```

This creates:
- `my_character.toml` - Dataset configuration
- `my_character_train.sh` - Training script

These files are gitignored to keep your project configurations local.
